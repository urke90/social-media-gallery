// ----------------------------------------------------------------

import { ISanityUser } from '@/models';

type PinDetailProps = {
  user: ISanityUser | null;
};

const PinDetail: React.FC<PinDetailProps> = (props) => {
  return <div>PinDetal</div>;
};

export default PinDetail;
