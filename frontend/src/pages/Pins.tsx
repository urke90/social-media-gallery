// ----------------------------------------------------------------

import { ISanityUser } from '@/models';

type Props = {
  user: ISanityUser | null;
};

const Pins = (props: Props) => {
  return <div>Pins</div>;
};

export default Pins;
